# PT-89: pmux space rm deletes saved space files.
session:
  command: pmux
  args: ["space", "--help"]
  cols: 80
  rows: 40
  env:
    TERM: xterm-256color

steps:
  - waitForText: {text: "pmux space", timeoutMs: 5000}
  - expectText: {text: "rm"}

artifacts:
  mode: always
  dir: e2e/artifacts
